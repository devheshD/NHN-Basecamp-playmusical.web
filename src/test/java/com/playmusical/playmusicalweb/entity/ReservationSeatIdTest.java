package com.playmusical.playmusicalweb.entity;

import org.junit.jupiter.api.Test;

import static io.github.benas.randombeans.api.EnhancedRandom.random;
import static org.junit.jupiter.api.Assertions.assertEquals;

class ReservationSeatIdTest {

/*    @Test
    void reservationDTOLombokTest() {
        ReservationSeatId origin = random(ReservationSeatId.class);

        ReservationSeatId builder = ReservationSeatId.builder()
                .reservationNo(origin.getReservationNo())
                .performanceNo(origin.getPerformanceNo())
                .seatNo(origin.getSeatNo())
                .build();

        ReservationSeatId setter = new ReservationSeatId();
        setter.setReservationNo(origin.getReservationNo());
        setter.setPerformanceNo(origin.getPerformanceNo());
        setter.setSeatNo(origin.getSeatNo());

        assertEquals(origin.getReservationNo(), builder.getReservationNo());
        assertEquals(origin.getReservationNo(), setter.getReservationNo());
    }*/
}